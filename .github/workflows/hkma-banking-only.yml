name: HKMA banking statistics only

on:
  push:
    branches: [research-hk-monetary-transmission]
    paths:
      - ".github/scripts/fetch_hkma_banking_only.py"
      - ".github/workflows/hkma-banking-only.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install pandas requests
      - run: python .github/scripts/fetch_hkma_banking_only.py
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hkma-banking-only
          path: research_output/hkma_banking_only
          if-no-files-found: error
          retention-days: 7
